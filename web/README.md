# pregame.lol web app

The full project README is at `../README.md` and covers setup, Supabase configuration, and how both games work.

Quick dev commands (run inside this `web` directory):
- Install deps: `npm install`
- Start dev server: `npm run dev`
- Lint: `npm run lint`
- Tests: `npm run test`
